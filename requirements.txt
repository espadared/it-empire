# The game and its server run on Python's standard library alone.
# These two are only needed when DATABASE_URL points at Postgres, which is how
# the hosted copy keeps saves through a restart. Without them (and without
# DATABASE_URL) everything still works, saving to a local SQLite file instead.
psycopg[binary]>=3.1
psycopg_pool>=3.2
